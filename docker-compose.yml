networks:
  careerpilot_network:
    driver: bridge

services:
  db:
    image: pgvector/pgvector:0.8.4-pg18-trixie
    container_name: careerpilot-db
    environment:
      POSTGRES_USER: careerpilot
      POSTGRES_PASSWORD: password
      POSTGRES_DB: careerpilot
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U careerpilot" ]
      interval: 5s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 512M
    networks:
      - careerpilot_network

  app:
    build: .
    container_name: careerpilot-app
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://careerpilot:password@db:5432/careerpilot
      - LANGSMITH_TRACING=true
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    command: >
      bash -c "uv run alembic upgrade head && uv run uvicorn app.main:app --host 0.0.0.0 --port 8000"
    networks:
      - careerpilot_network

  jdl-daemon:
    build: .
    container_name: careerpilot-jdl-daemon
    environment:
      - DATABASE_URL=postgresql+asyncpg://careerpilot:password@db:5432/careerpilot
      - LANGSMITH_TRACING=true
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    command: uv run python -m app.scripts.jdl_daemon
    deploy:
      resources:
        limits:
          memory: 256M
    networks:
      - careerpilot_network

#  prometheus:
#    image: prom/prometheus:latest
#    container_name: careerpilot-prometheus
#    ports:
#      - "9090:9090"
#    volumes:
#      - ./prometheus.yml:/etc/prometheus/prometheus.yml
#    command:
#      - '--config.file=/etc/prometheus/prometheus.yml'
#    depends_on:
#      - app
#    networks:
#      - careerpilot_network

volumes:
  postgres_data:
